[phases.setup]
nixPkgs = ["php84", "php84Extensions.pdo", "php84Extensions.pdo_pgsql", "php84Extensions.mbstring", "php84Extensions.tokenizer", "php84Extensions.xml", "php84Extensions.ctype", "php84Extensions.fileinfo", "php84Extensions.curl", "php84Extensions.zip", "php84Extensions.bcmath", "php84Extensions.gd", "php84Extensions.intl", "php84Extensions.redis", "nodejs_20", "nodePackages.pnpm"]

[phases.install]
cmds = [
  "composer install --no-dev --optimize-autoloader --no-interaction --no-progress",
  "pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "pnpm build"
]

[start]
cmd = "php artisan config:clear 2>/dev/null; php artisan route:clear 2>/dev/null; php artisan view:clear 2>/dev/null; php artisan storage:link --force 2>/dev/null; php artisan migrate --force 2>/dev/null; php artisan db:seed --force 2>/dev/null; exec php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
